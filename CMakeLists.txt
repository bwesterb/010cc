CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(010cc)

ADD_EXECUTABLE(010cc 010cc.cpp)

FIND_PACKAGE(Boost 1.40 REQUIRED)

TARGET_LINK_LIBRARIES(010cc ${Boost_LIBRARIES})
INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIR})

list(APPEND CMAKE_CXX_FLAGS "-Wall -std=c++0x -O3 ${CMAKE_CXX_FLAGS}")
